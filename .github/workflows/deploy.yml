name: Deploy API

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger server deploy webhook
        run: |
          curl -f -s "https://api.stalwartzm.com/deploy.php?token=${{ secrets.DEPLOY_SECRET }}"
